%h1 Apprentice IO breakable toy: Home
%p= link_to "New Category", new_category_path if can? :create, Category

%table
  %tbody
    - @categories.each do |category|
      %tr
        %td= link_to category.name, category_topics_path(category)
        %td= link_to "Edit", edit_category_path(category) if can? :update, Category
        %td= link_to 'Delete', category, method: :delete, id: "delete-#{category.name}" if can? :destroy, Category
