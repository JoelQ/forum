%h1 Apprentice IO breakable toy: Home
%p= link_to "New Category", new_category_path if can? :create, Category

%table
  %tbody
    - @categories.each do |category|
      %tr
        %td= link_to category.name, category_topics_path(category)
        %td= authorized_only_edit_link edit_category_path(category), category
        %td= authorized_only_delete_link category_path(category), category
